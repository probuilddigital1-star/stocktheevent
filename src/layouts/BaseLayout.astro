---
import '../styles/global.css';

interface BreadcrumbItem {
  name: string;
  url: string;
}

interface Props {
  title: string;
  description: string;
  canonicalUrl?: string;
  ogImage?: string;
  breadcrumbs?: BreadcrumbItem[];
}

const { title, description, canonicalUrl, ogImage = '/og-default.png', breadcrumbs } = Astro.props;
const siteUrl = 'https://stocktheevent.com';

// Organization Schema (sitewide)
const organizationSchema = {
  "@context": "https://schema.org",
  "@type": "Organization",
  "name": "StockTheEvent",
  "url": siteUrl,
  "logo": `${siteUrl}/logo.png`,
  "description": "Free food and drink calculators for party planning. Caterer-tested formulas for weddings, birthdays, Super Bowl parties, and more.",
  "foundingDate": "2024",
  "sameAs": [],
  "contactPoint": {
    "@type": "ContactPoint",
    "contactType": "customer service",
    "url": `${siteUrl}/about`
  }
};

// WebSite Schema (for sitelinks search box)
const websiteSchema = {
  "@context": "https://schema.org",
  "@type": "WebSite",
  "name": "StockTheEvent",
  "url": siteUrl,
  "description": "Free party food and drink calculators",
  "potentialAction": {
    "@type": "SearchAction",
    "target": `${siteUrl}/calculators?q={search_term_string}`,
    "query-input": "required name=search_term_string"
  }
};

// BreadcrumbList Schema (if breadcrumbs provided)
const breadcrumbSchema = breadcrumbs ? {
  "@context": "https://schema.org",
  "@type": "BreadcrumbList",
  "itemListElement": breadcrumbs.map((item, index) => ({
    "@type": "ListItem",
    "position": index + 1,
    "name": item.name,
    "item": item.url.startsWith('http') ? item.url : `${siteUrl}${item.url}`
  }))
} : null;
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <meta name="author" content="StockTheEvent" />

    <title>{title}</title>

    {canonicalUrl && <link rel="canonical" href={canonicalUrl} />}

    <!-- Open Graph -->
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:type" content="website" />
    <meta property="og:image" content={`${siteUrl}${ogImage}`} />
    {canonicalUrl && <meta property="og:url" content={canonicalUrl} />}

    <!-- Twitter -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={`${siteUrl}${ogImage}`} />

    <!-- Favicon -->
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />

    <!-- Google Fonts - Playfair Display for headlines -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@600;700;800&display=swap" rel="stylesheet" />

    <!-- Slot for page-specific head content (schema, etc) -->
    <slot name="head" />

    <!-- Sitewide Schema.org structured data -->
    <script type="application/ld+json" set:html={JSON.stringify(organizationSchema)} />
    <script type="application/ld+json" set:html={JSON.stringify(websiteSchema)} />
    {breadcrumbSchema && <script type="application/ld+json" set:html={JSON.stringify(breadcrumbSchema)} />}
  </head>
  <body class="min-h-screen bg-slate-50 text-slate-900 antialiased">
    <!-- Header -->
    <header class="bg-white/95 backdrop-blur-md border-b border-slate-200/50 sticky top-0 z-50 shadow-sm">
      <nav class="max-w-6xl mx-auto px-4 py-4 flex items-center justify-between">
        <a href="/" class="flex items-center gap-2 text-xl font-bold text-slate-900 hover:text-emerald-600 transition-colors group">
          <span class="text-3xl group-hover:scale-110 transition-transform">🍾</span>
          <span class="font-display">StockTheEvent</span>
        </a>
        <!-- Desktop Nav -->
        <div class="hidden sm:flex items-center gap-8 text-sm font-medium">
          <a href="/calculators" class="text-slate-600 hover:text-emerald-600 transition-colors">Drinks</a>
          <a href="/food/pizza-for-birthday-party-50-guests" class="text-slate-600 hover:text-emerald-600 transition-colors">Food</a>
          <a href="/party/super-bowl-party-calculator" class="text-slate-600 hover:text-emerald-600 transition-colors">Seasonal</a>
          <a href="/about" class="text-slate-600 hover:text-emerald-600 transition-colors">About</a>
          <a href="/" class="bg-gradient-to-r from-amber-500 to-orange-500 text-white px-4 py-2 rounded-lg hover:shadow-lg hover:scale-105 transition-all">
            Try Calculator
          </a>
        </div>
        <!-- Mobile Menu Button -->
        <button id="mobile-menu-btn" class="sm:hidden p-2 text-slate-600 hover:text-emerald-600" aria-label="Menu">
          <svg id="menu-icon" xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
          </svg>
          <svg id="close-icon" xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 hidden" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
          </svg>
        </button>
      </nav>
      <!-- Mobile Menu -->
      <div id="mobile-menu" class="hidden sm:hidden bg-white border-t border-slate-100 px-4 py-4">
        <div class="flex flex-col gap-4 text-base font-medium">
          <a href="/calculators" class="text-slate-600 hover:text-emerald-600 transition-colors py-2">Drink Calculators</a>
          <a href="/food/pizza-for-birthday-party-50-guests" class="text-slate-600 hover:text-emerald-600 transition-colors py-2">Food Calculators</a>
          <a href="/party/super-bowl-party-calculator" class="text-slate-600 hover:text-emerald-600 transition-colors py-2">Seasonal Parties</a>
          <a href="/about" class="text-slate-600 hover:text-emerald-600 transition-colors py-2">About</a>
          <a href="/" class="bg-gradient-to-r from-amber-500 to-orange-500 text-white px-4 py-3 rounded-lg text-center font-semibold">
            Try Calculator
          </a>
        </div>
      </div>
    </header>

    <script>
      const menuBtn = document.getElementById('mobile-menu-btn');
      const mobileMenu = document.getElementById('mobile-menu');
      const menuIcon = document.getElementById('menu-icon');
      const closeIcon = document.getElementById('close-icon');

      menuBtn?.addEventListener('click', () => {
        mobileMenu?.classList.toggle('hidden');
        menuIcon?.classList.toggle('hidden');
        closeIcon?.classList.toggle('hidden');
      });
    </script>

    <!-- Main Content -->
    <main>
      <slot />
    </main>

    <!-- Footer -->
    <footer class="bg-gradient-to-b from-slate-900 to-slate-950 text-slate-400">
      <div class="max-w-6xl mx-auto px-4 py-16">
        <div class="grid md:grid-cols-4 gap-10">
          <!-- Brand -->
          <div class="md:col-span-2">
            <div class="flex items-center gap-3 text-xl font-bold text-white mb-4">
              <span class="text-3xl">🍾</span>
              <span class="font-display">StockTheEvent</span>
            </div>
            <p class="text-sm leading-relaxed mb-6">
              Food & drink calculators backed by caterer-tested formulas.
              Never run out (or over-buy) for your event again.
            </p>
            <div class="flex items-center gap-2 text-sm">
              <span class="inline-block w-2 h-2 bg-emerald-500 rounded-full animate-pulse"></span>
              <span class="text-emerald-400">2,000+ calculators ready</span>
            </div>
          </div>

          <!-- Seasonal Parties -->
          <div>
            <h4 class="text-white font-semibold mb-5 flex items-center gap-2">
              <span class="w-8 h-px bg-gradient-to-r from-amber-500 to-orange-500"></span>
              Seasonal Parties
            </h4>
            <ul class="space-y-3 text-sm">
              <li><a href="/party/super-bowl-party-calculator" class="hover:text-emerald-400 transition-colors flex items-center gap-2">
                <span>🏈</span> Super Bowl Party
              </a></li>
              <li><a href="/party/graduation-party-calculator" class="hover:text-emerald-400 transition-colors flex items-center gap-2">
                <span>🎓</span> Graduation Party
              </a></li>
              <li><a href="/party/fourth-of-july-party-calculator" class="hover:text-emerald-400 transition-colors flex items-center gap-2">
                <span>🎆</span> 4th of July Party
              </a></li>
              <li><a href="/party/christmas-party-calculator" class="hover:text-emerald-400 transition-colors flex items-center gap-2">
                <span>🎄</span> Christmas Party
              </a></li>
            </ul>
          </div>

          <!-- Food & Drinks -->
          <div>
            <h4 class="text-white font-semibold mb-5 flex items-center gap-2">
              <span class="w-8 h-px bg-gradient-to-r from-amber-500 to-orange-500"></span>
              Food & Drinks
            </h4>
            <ul class="space-y-3 text-sm">
              <li><a href="/food/wings-for-super-bowl-party-50-guests" class="hover:text-emerald-400 transition-colors flex items-center gap-2">
                <span>🍗</span> Wings Calculator
              </a></li>
              <li><a href="/food/pizza-for-birthday-party-50-guests" class="hover:text-emerald-400 transition-colors flex items-center gap-2">
                <span>🍕</span> Pizza Calculator
              </a></li>
              <li><a href="/calculators?item=beer" class="hover:text-emerald-400 transition-colors flex items-center gap-2">
                <span>🍺</span> Beer Calculators
              </a></li>
              <li><a href="/calculators?item=wine" class="hover:text-emerald-400 transition-colors flex items-center gap-2">
                <span>🍷</span> Wine Calculators
              </a></li>
            </ul>
          </div>
        </div>

        <!-- Affiliate Disclosure -->
        <div class="border-t border-slate-800 mt-12 pt-8">
          <p class="text-xs text-slate-500 text-center mb-4 max-w-2xl mx-auto">
            <strong>Affiliate Disclosure:</strong> As an Amazon Associate, we earn from qualifying purchases.
            This site contains affiliate links, and we may receive a small commission at no extra cost to you.
          </p>
        </div>

        <!-- Legal Links & Copyright -->
        <div class="border-t border-slate-800 mt-6 pt-6 text-sm">
          <div class="flex flex-col sm:flex-row items-center justify-between gap-4">
            <div class="flex items-center gap-6 text-slate-500">
              <a href="/privacy" class="hover:text-emerald-400 transition-colors">Privacy Policy</a>
              <a href="/terms" class="hover:text-emerald-400 transition-colors">Terms of Service</a>
              <a href="/about" class="hover:text-emerald-400 transition-colors">About</a>
            </div>
            <p class="text-slate-500">&copy; {new Date().getFullYear()} StockTheEvent. Drink responsibly. 21+</p>
          </div>
        </div>
      </div>
    </footer>
  </body>
</html>
