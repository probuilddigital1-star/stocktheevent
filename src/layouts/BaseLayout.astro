---
import '../styles/global.css';

interface BreadcrumbItem {
  name: string;
  url: string;
}

interface Props {
  title: string;
  description: string;
  canonicalUrl?: string;
  ogImage?: string;
  breadcrumbs?: BreadcrumbItem[];
}

const { title, description, canonicalUrl, ogImage = '/og-default.png', breadcrumbs } = Astro.props;
const siteUrl = 'https://stocktheevent.com';

// Organization Schema (sitewide)
const organizationSchema = {
  "@context": "https://schema.org",
  "@type": "Organization",
  "name": "StockTheEvent",
  "url": siteUrl,
  "logo": `${siteUrl}/logo.png`,
  "description": "Free food and drink calculators for party planning. Caterer-tested formulas for weddings, birthdays, Super Bowl parties, and more.",
  "foundingDate": "2024",
  "sameAs": [],
  "contactPoint": {
    "@type": "ContactPoint",
    "contactType": "customer service",
    "url": `${siteUrl}/about`
  }
};

// WebSite Schema (for sitelinks search box)
const websiteSchema = {
  "@context": "https://schema.org",
  "@type": "WebSite",
  "name": "StockTheEvent",
  "url": siteUrl,
  "description": "Free party food and drink calculators",
  "potentialAction": {
    "@type": "SearchAction",
    "target": `${siteUrl}/calculators?q={search_term_string}`,
    "query-input": "required name=search_term_string"
  }
};

// BreadcrumbList Schema (if breadcrumbs provided)
const breadcrumbSchema = breadcrumbs ? {
  "@context": "https://schema.org",
  "@type": "BreadcrumbList",
  "itemListElement": breadcrumbs.map((item, index) => ({
    "@type": "ListItem",
    "position": index + 1,
    "name": item.name,
    "item": item.url.startsWith('http') ? item.url : `${siteUrl}${item.url}`
  }))
} : null;
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <meta name="author" content="StockTheEvent" />
    <meta name="theme-color" content="#dfb22a" />

    <title>{title}</title>

    {canonicalUrl && <link rel="canonical" href={canonicalUrl} />}

    <!-- Open Graph -->
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:type" content="website" />
    <meta property="og:image" content={`${siteUrl}${ogImage}`} />
    {canonicalUrl && <meta property="og:url" content={canonicalUrl} />}

    <!-- Twitter -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={`${siteUrl}${ogImage}`} />

    <!-- Favicon -->
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />

    <!-- Google Fonts - Golden Hour Design System -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Manrope:wght@400;500;600;700&family=Noto+Serif:ital,wght@0,400;0,600;0,700;1,400&family=Space+Grotesk:wght@300;400;500;700&display=swap" rel="stylesheet" />

    <!-- Material Symbols for icons -->
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200&display=swap" />

    <!-- Slot for page-specific head content (schema, etc) -->
    <slot name="head" />

    <!-- Sitewide Schema.org structured data -->
    <script type="application/ld+json" set:html={JSON.stringify(organizationSchema)} />
    <script type="application/ld+json" set:html={JSON.stringify(websiteSchema)} />
    {breadcrumbSchema && <script type="application/ld+json" set:html={JSON.stringify(breadcrumbSchema)} />}
  </head>
  <body class="min-h-screen gradient-page font-body text-[var(--chocolate)] antialiased relative">
    <!-- Background atmosphere -->
    <div class="blob-orange" aria-hidden="true"></div>
    <div class="blob-cream" aria-hidden="true"></div>

    <!-- Header -->
    <header class="glass-nav sticky top-0 z-50 border-b border-[var(--primary-10)]">
      <nav class="max-w-6xl mx-auto px-4 py-4 flex items-center justify-between">
        <!-- Logo -->
        <a href="/" class="flex items-center gap-3 group">
          <span class="text-3xl group-hover:scale-110 transition-transform">🥂</span>
          <div class="flex flex-col">
            <span class="font-display text-xl font-bold text-[var(--chocolate)] group-hover:text-[var(--primary-dark)] transition-colors">
              StockTheEvent
            </span>
            <span class="h-0.5 w-8 bg-[var(--primary)] rounded-full"></span>
          </div>
        </a>

        <!-- Desktop Nav -->
        <div class="hidden sm:flex items-center gap-8 text-sm font-medium">
          <a href="/calculators" class="text-[var(--chocolate-light)] hover:text-[var(--primary-dark)] transition-colors">Drinks</a>
          <a href="/food/pizza-for-birthday-party-50-guests" class="text-[var(--chocolate-light)] hover:text-[var(--primary-dark)] transition-colors">Food</a>
          <a href="/party/super-bowl-party-calculator" class="text-[var(--chocolate-light)] hover:text-[var(--primary-dark)] transition-colors">Seasonal</a>
          <a href="/about" class="text-[var(--chocolate-light)] hover:text-[var(--primary-dark)] transition-colors">About</a>
          <a href="/" class="forest-gradient text-white px-5 py-2.5 rounded-xl font-semibold hover:scale-105 transition-transform">
            Try Calculator
          </a>
        </div>

        <!-- Mobile Menu Button -->
        <button id="mobile-menu-btn" class="sm:hidden p-2 text-[var(--chocolate)] hover:text-[var(--primary)]" aria-label="Menu">
          <svg id="menu-icon" xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
          </svg>
          <svg id="close-icon" xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 hidden" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
          </svg>
        </button>
      </nav>

      <!-- Mobile Menu -->
      <div id="mobile-menu" class="hidden sm:hidden bg-white/95 backdrop-blur-lg border-t border-[var(--primary-10)] px-4 py-4">
        <div class="flex flex-col gap-4 text-base font-medium">
          <a href="/calculators" class="text-[var(--chocolate)] hover:text-[var(--primary-dark)] transition-colors py-2">Drink Calculators</a>
          <a href="/food/pizza-for-birthday-party-50-guests" class="text-[var(--chocolate)] hover:text-[var(--primary-dark)] transition-colors py-2">Food Calculators</a>
          <a href="/party/super-bowl-party-calculator" class="text-[var(--chocolate)] hover:text-[var(--primary-dark)] transition-colors py-2">Seasonal Parties</a>
          <a href="/about" class="text-[var(--chocolate)] hover:text-[var(--primary-dark)] transition-colors py-2">About</a>
          <a href="/" class="forest-gradient text-white px-4 py-3 rounded-xl text-center font-semibold">
            Try Calculator
          </a>
        </div>
      </div>
    </header>

    <script>
      const menuBtn = document.getElementById('mobile-menu-btn');
      const mobileMenu = document.getElementById('mobile-menu');
      const menuIcon = document.getElementById('menu-icon');
      const closeIcon = document.getElementById('close-icon');

      menuBtn?.addEventListener('click', () => {
        mobileMenu?.classList.toggle('hidden');
        menuIcon?.classList.toggle('hidden');
        closeIcon?.classList.toggle('hidden');
      });
    </script>

    <!-- Main Content -->
    <main>
      <slot />
    </main>

    <!-- Footer -->
    <footer class="bg-[var(--chocolate-dark)] text-[var(--champagne-200)]">
      <div class="max-w-6xl mx-auto px-4 py-16">
        <div class="grid md:grid-cols-4 gap-10">
          <!-- Brand -->
          <div class="md:col-span-2">
            <div class="flex items-center gap-3 text-xl font-bold text-white mb-4">
              <span class="text-3xl">🥂</span>
              <div class="flex flex-col">
                <span class="font-display">StockTheEvent</span>
                <span class="h-0.5 w-8 bg-[var(--primary)] rounded-full"></span>
              </div>
            </div>
            <p class="text-sm leading-relaxed mb-6 text-[var(--champagne-300)]">
              Food & drink calculators backed by caterer-tested formulas.
              Never run out (or over-buy) for your event again.
            </p>
            <div class="flex items-center gap-2 text-sm">
              <span class="inline-block w-2 h-2 bg-[var(--forest)] rounded-full animate-pulse"></span>
              <span class="text-[var(--forest-light)]">2,000+ calculators ready</span>
            </div>
          </div>

          <!-- Seasonal Parties -->
          <div>
            <h4 class="text-white font-semibold mb-5 flex items-center gap-2">
              <span class="w-8 h-px bg-[var(--primary)]"></span>
              Seasonal Parties
            </h4>
            <ul class="space-y-3 text-sm">
              <li><a href="/party/super-bowl-party-calculator" class="hover:text-[var(--primary)] transition-colors flex items-center gap-2">
                <span>🏈</span> Super Bowl Party
              </a></li>
              <li><a href="/party/graduation-party-calculator" class="hover:text-[var(--primary)] transition-colors flex items-center gap-2">
                <span>🎓</span> Graduation Party
              </a></li>
              <li><a href="/party/fourth-of-july-party-calculator" class="hover:text-[var(--primary)] transition-colors flex items-center gap-2">
                <span>🎆</span> 4th of July Party
              </a></li>
              <li><a href="/party/christmas-party-calculator" class="hover:text-[var(--primary)] transition-colors flex items-center gap-2">
                <span>🎄</span> Christmas Party
              </a></li>
            </ul>
          </div>

          <!-- Food & Drinks -->
          <div>
            <h4 class="text-white font-semibold mb-5 flex items-center gap-2">
              <span class="w-8 h-px bg-[var(--primary)]"></span>
              Food & Drinks
            </h4>
            <ul class="space-y-3 text-sm">
              <li><a href="/food/wings-for-super-bowl-party-50-guests" class="hover:text-[var(--primary)] transition-colors flex items-center gap-2">
                <span>🍗</span> Wings Calculator
              </a></li>
              <li><a href="/food/pizza-for-birthday-party-50-guests" class="hover:text-[var(--primary)] transition-colors flex items-center gap-2">
                <span>🍕</span> Pizza Calculator
              </a></li>
              <li><a href="/calculators?item=beer" class="hover:text-[var(--primary)] transition-colors flex items-center gap-2">
                <span>🍺</span> Beer Calculators
              </a></li>
              <li><a href="/calculators?item=wine" class="hover:text-[var(--primary)] transition-colors flex items-center gap-2">
                <span>🍷</span> Wine Calculators
              </a></li>
            </ul>
          </div>
        </div>

        <!-- Affiliate Disclosure -->
        <div class="border-t border-[var(--chocolate)] mt-12 pt-8">
          <p class="text-xs text-[var(--chocolate-light)] text-center mb-4 max-w-2xl mx-auto">
            <strong>Affiliate Disclosure:</strong> As an Amazon Associate, we earn from qualifying purchases.
            This site contains affiliate links, and we may receive a small commission at no extra cost to you.
          </p>
        </div>

        <!-- Legal Links & Copyright -->
        <div class="border-t border-[var(--chocolate)] mt-6 pt-6 text-sm">
          <div class="flex flex-col sm:flex-row items-center justify-between gap-4">
            <div class="flex items-center gap-6 text-[var(--chocolate-light)]">
              <a href="/privacy" class="hover:text-[var(--primary)] transition-colors">Privacy Policy</a>
              <a href="/terms" class="hover:text-[var(--primary)] transition-colors">Terms of Service</a>
              <a href="/about" class="hover:text-[var(--primary)] transition-colors">About</a>
            </div>
            <p class="text-[var(--chocolate-light)]">&copy; {new Date().getFullYear()} StockTheEvent. Drink responsibly. 21+</p>
          </div>
        </div>
      </div>
    </footer>
  </body>
</html>
